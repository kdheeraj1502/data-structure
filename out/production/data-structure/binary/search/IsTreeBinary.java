package binary.search;

public class IsTreeBinary {
   public static boolean isBinary(Node root) {
      int[] count = new int[1];
      return isBinary(root, count);
   }

   private static boolean isBinary(Node root, int[] count) {
      return false;
   }
}
